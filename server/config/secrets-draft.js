// TODO: copy this file here and rename it to "secrets.js", after that complete the bellow settings to run the server
const MONGO_URI = "";
const JWT_SECRET = "";

const SUPER_ADMIN_DEFAULTS = {
  NAME: "",
  EMAIL: "",
  PASSWORD: "",
};

const MAILER_AUTH = {
  USER: "",
  PASS: "",
};

module.exports = {
  JWT_SECRET,
  MONGO_URI,
  SUPER_ADMIN_DEFAULTS,
  MAILER_AUTH,
};
